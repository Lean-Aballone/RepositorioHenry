function duplicarElementos(array) {
  // Duplica (multiplica x2) cada elemento del array de números.
  // Devuelve un array con los duplicados.
  // Tu código:
  return Array.from(array,(element) => element*2);
}
  

module.exports = duplicarElementos;
